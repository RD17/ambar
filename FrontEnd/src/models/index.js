import * as hitsModel from './hitsModel'
import * as crawlersModel from './crawlersModel'
import * as folderHitsModel from './folderHitsModel'

export {  
    hitsModel,
    crawlersModel,
    folderHitsModel
}